import OneCar from "./OneCar";

const Like = ({ cars }) => {
    return (
      <div className="liked-container">
     {/* {cars.map((c) => (
          <OneCar car={c} key={c.id} inCart={0} />
        ))}
     */ }  
      </div>
    );
  };
  
  export default Like;